{
  "name": "uri-moa-wallet-demo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node -e \"console.log('Static demo: no build step required')\"",
    "compile": "hardhat compile",
    "deploy:giwa": "hardhat run scripts/deploy.js --network giwaSepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-ethers": "^3.0.8",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.19"
  }
}
